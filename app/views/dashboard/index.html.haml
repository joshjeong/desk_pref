%div.navbar
  %span#front-title.transition> Desk
  %span#back-title.transition Space
  %span#sign-out
    Signed in as #{current_user.email}.
    \#{link_to "Sign Out", destroy_user_session_path, :method => :delete}

= render :partial => 'desks/new'
